import React, { FC, useLayoutEffect, useRef } from 'react';


import styles from './placeholder.module.css';


export const KotlinEffectPlaceholder: FC = () => {

    return (
        <section className={styles.section}>
          placeholder
        </section>
    );
};
